name: Call Reusable Autobumper
on:
  workflow_dispatch: # Allows manual triggering of the workflow

jobs:
  call-autobumper:
    uses: akiioto/test-infra/.github/workflows/reusable-autobumper.yml@bumper
    with:
      GCP_PROJECT_ID: 'sap-kyma-neighbors-dev'
